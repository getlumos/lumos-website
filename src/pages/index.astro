---
import Layout from '../layouts/Layout.astro';
import NavbarAstro from '../components/NavbarAstro.astro';
import FooterAstro from '../components/FooterAstro.astro';
---

<Layout title="LUMOS - Type-Safe Schema Language for Solana">
  <NavbarAstro slot="navbar" />

  <div class="pt-16">
    <!-- Hero Section -->
    <section class="relative min-h-[80vh] flex items-center justify-center overflow-hidden" style="background: var(--gradient-hero);">
      <div class="container mx-auto px-4 text-center">
        <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full border border-primary/30 bg-primary/10 text-sm text-primary mb-8">
          <span class="relative flex h-2 w-2">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
            <span class="relative inline-flex rounded-full h-2 w-2 bg-primary"></span>
          </span>
          v0.3.0 Released
        </div>

        <h1 class="text-4xl md:text-6xl lg:text-7xl font-bold mb-6 leading-tight">
          Type-Safe Schemas for
          <span class="bg-gradient-to-r from-purple-500 to-purple-400 bg-clip-text text-transparent"> Solana</span>
        </h1>

        <p class="text-xl md:text-2xl text-muted-foreground max-w-3xl mx-auto mb-12">
          Write data structures once in <code class="text-primary">.lumos</code> syntax.
          Generate production-ready <strong>Rust</strong> + <strong>TypeScript</strong> with guaranteed Borsh compatibility.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a
            href="https://docs.lumos-lang.org/getting-started/quick-start"
            class="inline-flex items-center justify-center rounded-lg text-lg font-semibold h-14 px-8 bg-primary text-primary-foreground hover:bg-primary/90 transition-colors"
          >
            Get Started
            <svg class="ml-2 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
            </svg>
          </a>
          <a
            href="/playground"
            class="inline-flex items-center justify-center rounded-lg text-lg font-semibold h-14 px-8 border border-input bg-background hover:bg-accent hover:text-accent-foreground transition-colors"
          >
            Try Playground
          </a>
        </div>

        <!-- Quick install -->
        <div class="mt-12">
          <p class="text-sm text-muted-foreground mb-2">Install via Cargo</p>
          <code class="inline-block px-4 py-2 rounded-lg bg-secondary text-sm font-mono">
            cargo install lumos-cli
          </code>
        </div>
      </div>

      <!-- Background decorations -->
      <div class="absolute inset-0 -z-10 overflow-hidden">
        <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-primary/20 rounded-full blur-3xl"></div>
        <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-accent/10 rounded-full blur-3xl"></div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="py-24 bg-secondary/20">
      <div class="container mx-auto px-4">
        <h2 class="text-3xl md:text-4xl font-bold text-center mb-4">
          Why LUMOS?
        </h2>
        <p class="text-xl text-muted-foreground text-center max-w-2xl mx-auto mb-16">
          Stop maintaining duplicate type definitions. Let LUMOS handle the complexity.
        </p>

        <div class="grid md:grid-cols-3 gap-8">
          <div class="p-6 rounded-xl bg-card border border-border">
            <div class="w-12 h-12 rounded-lg bg-primary/20 flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-2">Type Safety</h3>
            <p class="text-muted-foreground">
              Catch type mismatches at compile time, not runtime. Guaranteed compatibility between Rust and TypeScript.
            </p>
          </div>

          <div class="p-6 rounded-xl bg-card border border-border">
            <div class="w-12 h-12 rounded-lg bg-primary/20 flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-2">Single Source of Truth</h3>
            <p class="text-muted-foreground">
              Define your schema once. Generate Rust, TypeScript, Python, Go, and Ruby from the same source.
            </p>
          </div>

          <div class="p-6 rounded-xl bg-card border border-border">
            <div class="w-12 h-12 rounded-lg bg-primary/20 flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </div>
            <h3 class="text-xl font-semibold mb-2">Anchor Integration</h3>
            <p class="text-muted-foreground">
              Works seamlessly with Anchor framework. Generated code includes all necessary derives and attributes.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Code Example -->
    <section class="py-24">
      <div class="container mx-auto px-4">
        <h2 class="text-3xl md:text-4xl font-bold text-center mb-4">
          Simple, Expressive Syntax
        </h2>
        <p class="text-xl text-muted-foreground text-center max-w-2xl mx-auto mb-16">
          Define your Solana data structures in clean, readable syntax.
        </p>

        <div class="max-w-4xl mx-auto">
          <div class="rounded-xl bg-card border border-border overflow-hidden">
            <div class="flex items-center gap-2 px-4 py-3 bg-secondary/50 border-b border-border">
              <div class="w-3 h-3 rounded-full bg-red-500"></div>
              <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
              <div class="w-3 h-3 rounded-full bg-green-500"></div>
              <span class="ml-2 text-sm text-muted-foreground">schema.lumos</span>
            </div>
            <pre class="p-6 overflow-x-auto"><code class="text-sm">{`#[solana]
#[account]
struct PlayerAccount {
    wallet: PublicKey,
    level: u16,
    experience: u64,
    equipped_items: [PublicKey],
}

#[solana]
enum GameState {
    Active,
    Paused,
    Finished,
}`}</code></pre>
          </div>
        </div>
      </div>
    </section>
  </div>

  <FooterAstro slot="footer" />
</Layout>
